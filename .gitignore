# local working notes (not part of the published plugin)
HANDOFF.md

# qa-explore runtime artifacts
/tmp/
*.har
*.zip
storageState.json
node_modules/
.DS_Store

# screenshots / reports are written to shotsDir (default /tmp/qa-explore), not the repo
