---
description: Living user/configuration manual — drive the happy path in setup order and capture annotated screenshots
argument-hint: "[--audience end-user|installer]"
---

Run the `qa-manual` skill at `${CLAUDE_PLUGIN_ROOT}/skills/qa-manual/SKILL.md`.

Read that SKILL.md now and follow it exactly.

User arguments (may be empty): $ARGUMENTS

Two human gates, both mandatory: the user approves the table of contents and audience up front, and reviews the assembled Markdown draft before anything is exported. Never publish auto-generated docs blind, and don't document a flow that has an open bug.
